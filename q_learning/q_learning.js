let can = document.getElementById('can')
let ctx = can.getContext('2d')
console.log(can)
console.log(ctx)

const width = can.width = 600
const height = can.height = 400

ctx.fillRect(0, 0, width, height)
const grid = [
    []
]


function draw(ctx) {
    
}


draw();